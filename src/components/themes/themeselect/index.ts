import { Theme } from "../../../lib/theme";
import { forest } from "./forest";
import { ocean } from "./ocean";
import { sunset } from "../../theme/sunset";
import { midnight } from "./midnight";

export const themes: Theme[] = [
  forest,
  ocean,
  sunset,
  midnight,
];

export { forest, ocean, sunset, midnight }; 